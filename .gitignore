# Agent configurations
.opencode/
opencode.json
AGENTS.md

# pi-lens caches and local reviews
.pi-lens/

# Local pi agent state (personal, not pushed to remote)
.pi/
scripts/booboo_append_slop.sh

# Internal planning docs (dev workflow only, not package artifacts)
BACKLOG.md
todo_plan.md

# Type checker config (personal tool config)
ty.toml

# Raw data (large CSVs, sometimes sensitive) — shipped examples allowed
data/raw/*.csv
!data/raw/.gitkeep
!data/raw/fertility_data.csv
!data/raw/nativity_analyticdata.csv

# Node
node_modules/
package.json
package-lock.json

# Reference implementations (not part of package)
# NOTE: moved up one level to ../reference_directories (Refactor_DOBBS/) to keep
# pi-lens /lens-booboo scans fast. Kept rule below in case dir is recreated here.
reference_directories/

# R / Quarto (package is pure Python — any .R / .qmd stragglers are local-only)
*.R
*.r
*.Rmd
*.qmd
*.Rproj
.Rhistory
.RData
.Rproj.user/
.Rlibrary/
renv/
renv.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/
.env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Results (generated, not source) — includes figs/ subdirs written by reporting.py
results/**
!results/.gitkeep

# Quarto/RMarkdown cache
*_cache/
*_files/
*.html

# Logs
*.log

# Coverage
.coverage
htmlcov/

# pytest
.pytest_cache/

# mypy
.mypy_cache/

# Scratch / subagent review artifacts (local-only, not package source)
progress.md
context.md
research.md
reporting_review.md
review-outcome-derivation.md
review_*.md
oracle_pyright_review.md
jama_supplement_analysis.md
pyrightconfig.json
page*.png

# Refactor baseline fixtures (local-only, deleted after verification)
tests/fixtures/pre_refactor_*
tests/fixtures/compare_posterior.py
